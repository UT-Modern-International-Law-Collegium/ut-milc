export type TopRes = {
  aboutUs: string;
  awards: string;
  joinUs: string;
};

export type DynamicRouteObj = {
  params: {
    year: string;
  };
}[];

export type AwardYearsRes = {
  year: number;
};
