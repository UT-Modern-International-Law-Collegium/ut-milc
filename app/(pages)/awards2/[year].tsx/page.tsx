'use client';

import { useEffect } from 'react';

const Page = ({ params }: { params: { year: string } }) => {
  const { year } = params;

  useEffect(() => {
    const f = async () => {};
  }, []);
};

export default Page;
